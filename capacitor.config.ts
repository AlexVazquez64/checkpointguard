import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.checkpointguard',
  appName: 'checkpointguard',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
